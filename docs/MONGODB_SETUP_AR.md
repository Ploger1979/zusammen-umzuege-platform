# 🛠️ إعداد قاعدة بيانات MongoDB Atlas (خطوة بخطوة)

لكي يعمل تسجيل الدخول وتسجيل البيانات في موقعك على Netlify، تحتاج إلى قاعدة بيانات سحابية. اتبع هذه الخطوات لإنشاء واحدة مجانية واحترافية.

---

### 1- إنشاء الحساب
1. اذهب إلى [mongodb.com/cloud/atlas/register](https://www.mongodb.com/cloud/atlas/register).
2. سجل حسابًا مجانيًا (يمكنك استخدام حساب Google).

### 2- إنشاء قاعدة البيانات (Cluster)
1. اختر الخطة المجانية **"Shared"** (أو **Free**).
2. اختر المزود (Provider) مثل **AWS** والمنطقة (Region) القريبة منك، مثلاً **Frankfurt** `eu-central-1`.
3. اضغط في الأسفل على زر **"Create Cluster"**.

### 3- إنشاء مستخدم وكلمة مرور (هام جداً!)
1. سيطلب منك إنشاء مستخدم لقاعدة البيانات ("Database Access").
2. **Username:** اكتب مثلاً `admin`.
3. **Password:** اكتب كلمة مرور قوية و**احفظها جيداً** (ستحتاجها لوضعها في الرابط).
4. اضغط على **"Create Database User"**.

### 4- السماح بالاتصال (Network Access)
1. اذهب إلى القائمة الجانبية واختر **"Network Access"**.
2. اضغط على **"Add IP Address"**.
3. اختر **"Allow Access from Anywhere"** (أو اكتب `0.0.0.0/0`).
   * *هذه الخطوة ضرورية جداً لكي يتمكن موقع Netlify من الاتصال بقاعدة البيانات.*
4. اضغط **Confirm**.

### 5- الحصول على رابط الربط (Connection String)
1. ارجع إلى لوحة التحكم الرئيسية (**Database** في القائمة).
2. عند الـ Cluster الذي أنشأته، اضغط على زر **"Connect"**.
3. اختر **"Drivers"**.
4. سيظهر لك رابط طويل يشبه هذا:
   ```text
   mongodb+srv://admin:<db_password>@cluster0.p8xyz.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
   ```
5. **انسخ هذا الرابط.**

### 6- وضعه في Netlify
1. افتح لوحة تحكم [Netlify Dashboard](https://app.netlify.com/).
2. اختر مشروعك **"Zusammen Umzüge"**.
3. اذهب إلى **"Site configuration"** ثم **"Environment variables"**.
4. اضغط على **"Add a variable"**.
5. **الاسم (Key):** `MONGODB_URI`
6. **القيمة (Value):** الصق الرابط الذي نسخته في الخطوة 5.
   * ⚠️ **تنبيه هام:** يجب أن تحذف كلمة `<db_password>` من الرابط وتكتب مكانها كلمة المرور الحقيقية التي اخترتها في الخطوة 3 (بدون الأقواس `< >`).
7. اضغط **"Save"**.

---

### ✅ انتهينا!
الآن، قم بإعادة نشر الموقع (Redeploy) على Netlify، وسيعمل تسجيل الدخول والتسجيل بشكل ممتاز! 🚀

**(إضافي) الربط ببرنامج Compass:**
يمكنك استخدام نفس الرابط ووضعه في برنامج **MongoDB Compass** على جهازك لرؤية البيانات والمستخدمين مباشرة من سطح المكتب.
